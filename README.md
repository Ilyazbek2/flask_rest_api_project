# Flask REST API Example

A simple REST API built with Flask and Flask-RESTful for managing a collection of books.

## 🚀 Features
- Get all books (`GET /api/books`)
- Add a new book (`POST /api/books`)

## 🧰 Technologies
- Flask
- Flask-RESTful
- Marshmallow

## ▶️ Run Locally

```bash
# 1. Clone repository
git clone https://github.com/<your-username>/flask_rest_api_project.git

# 2. Enter folder
cd flask_rest_api_project

# 3. Create and activate virtual environment
python -m venv venv
venv\Scripts\activate  # Windows

# 4. Install dependencies
pip install -r requirements.txt

# 5. Run server
python run.py
